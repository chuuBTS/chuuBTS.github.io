Store the paper framework images here, using these exact filenames:

- `alpha-vis-framework.png`
- `nvbench2-framework.png`

The Research page automatically displays each image once its file is present.
Images scale to the page width; clicking opens the full-resolution file.
Export the original figures as PNG, preferably at least 1600 pixels wide for readable labels.
If you use another file format, update the corresponding include path in `_pages/projects.md`.
